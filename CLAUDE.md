# CLAUDE.md

Claude Code loads this file at launch. The instructions and the project context live in
`AGENTS.md` and `agent-docs/` so every coding agent reads the same thing; the import
below pulls them into context (see `agent-docs/README.md` for the reading order).

@AGENTS.md
